compilation:
  cloud_properties:
    instance_type: c1.medium
    availability_zone: (( properties.template_only.aws.availability_zone2 ))

networks:
  - name: services_v1_mysql_1
    type: (( merge ))
    subnets:
    - name: default_unused
      range: (( merge ))
      reserved: (( merge ))
      static: (( merge ))
      gateway: (( merge ))
      dns: (( merge ))
      cloud_properties:
        security_groups:
        - cf
        subnet: (( properties.template_only.aws.subnet_ids.services_v1_mysql_1 ))

resource_pools:
  - name: services-small
    cloud_properties:
      instance_type: m1.small
      availability_zone: (( properties.template_only.aws.availability_zone2 ))

properties:
  template_only: (( merge ))
